% This script calculate area of rectangle with given user-defined base and height

base=input('Enter the base of rectangle in inches: ')
height=input('Enter the height of rectangle in inches: ')
Name=input('Enter the name of rectangle: ','s')
Area=base * height;

% to display the area use "disp" function
disp(Area)
